=== FRONTEND ===

1. Frontend dùng framework/bundler gì?
   - Vue Frameworks
   - Vite
   - TypeScript

2. Frontend chạy như thế nào?
   - yarn install
   - yarn dev
   - Frontend chạy ở port 5174 (vite.config.ts)
   - Có cần file .env không? -> Không cần .env vì frontend gọi API qua Vite proxy http://localhost:3000

3. Entry point và cấu trúc chính nằm ở đâu?
   - File main/index là file nào? -> frontend\src\main.ts
   - Component gốc là App nào? ->  frontend\src\App.vue
   - Pages/views nằm ở đâu? ->  frontend\src\App.vue
   - Components dùng lại nằm ở đâu? -> frontend\src\components

4. Routing được tổ chức thế nào?
   - Có dùng router không? KHÔNG
   - Route được khai báo ở file nào?
   - URL nào tương ứng với page nào?

5. Frontend gọi backend bằng cách nào?
   - Dùng fetch hay axios? frontend/src/App.vue chỉ dùng fetch
   - API client nằm ở file nào? không có, logic gọi API được viết trực tiếp trong App.vue
   - Base URL lấy từ .env hay hard-code? hard code "/api" trong App.vue
   - Endpoint có prefix /api không? có, tất cả request phải dùng /api

6. State được quản lý ở đâu?
   - Dùng useState/useReducer/Context/Redux/Zustand? State được quản lý bằng Vue Composition API (ref, computed)
   - State nào là server data? todoList lấy từ /api
   - State nào là UI state? isLoading, isFetching, isDeletingAll, isUpdating, isShowingToast, filter, và "task", "dueDate"
   - Có lưu gì vào localStorage/sessionStorage không?  theme được lưu vào localStorage (Themeswitcher.vue)

7. Luồng dữ liệu chính hoạt động thế nào?
   - Khi user bấm nút/submit form, handler nào chạy?
   - Handler gọi API nào?
   - Payload gửi đi gồm gì?
   - Response được lưu vào state nào?
   - Component nào re-render?

8. Form và validation xử lý ở đâu?
   - Form dùng controlled input hay thư viện form?
   - Validate ở frontend hay chỉ backend?
   - Khi input sai thì UI báo lỗi thế nào?

9. Loading, error, empty state xử lý thế nào?
   - Khi API đang chạy có loading không?
   - Khi API lỗi có message không?
   - Khi không có data có empty state không?
   - Có phân biệt lỗi 400/401/403/404/500 không?

10. Authentication có không?
    - Có login/logout/register không?
    - Token lưu ở đâu?
    - Token được gắn vào request bằng cách nào?
    - Có protected route không?

11. Styling dùng gì?
    - CSS/Tailwind/SCSS/CSS Modules/component library?
    - Style global nằm ở đâu?
    - Responsive xử lý thế nào?

12. Build và deploy thế nào?
    - Lệnh build là gì?
    - Output nằm ở dist/ hay build/?
    - Production API URL cấu hình ở đâu?
    - Frontend được serve bằng Nginx, Node server, hay platform khác?

13. Có risk security/config nào không?
    - Có secret/API key/token bị hard-code không?
    - Có dùng dangerouslySetInnerHTML/innerHTML không?
    - Token lưu localStorage có rủi ro XSS không?
    - Frontend có lộ config nhạy cảm không?

14. Có lỗi performance phổ biến không?
    - API có bị gọi lặp vô hạn không?
    - useEffect dependency có đúng không?
    - Component có re-render quá nhiều không?
    - Asset/bundle có quá nặng không?

15. Tóm lại: một user action đi qua những lớp nào?
    UI event -> handler -> state/form data -> API call -> backend response -> state update -> UI re-render